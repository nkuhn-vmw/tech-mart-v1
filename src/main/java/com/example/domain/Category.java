package com.example.domain;

/**
 * Placeholder class for Category to avoid duplicate entity definitions.
 * The functional JPA entity resides in com.example.entity.Category.
 */
public class Category {
    // No fields or JPA annotations; used only for legacy imports if any.
}
